{
  "type": "object",
  "properties": {
    "name": {
      "description": "Name of the Pod",
      "type": "string"
    },
    "namespace": {
      "description": "Namespace to get the Pod from",
      "type": "string"
    }
  },
  "required": ["name"],
  "title": "pods_getArguments",
  "description": "Get a Kubernetes Pod in the current or provided namespace with the provided name"
}